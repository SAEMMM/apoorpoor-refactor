"use client";

import { Box, styled } from "@mui/material";

export const Header = styled(Box)(() => ({
  width: "100%",
  position: "relative",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
}));
